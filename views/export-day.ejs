<%- include('partials/header') %>

<section class="max-w-5xl mx-auto mt-10 px-4">
  <h2 class="text-2xl font-bold mb-4">📦 Token Archive for <%= date %></h2>
  <p class="text-gray-600 mb-6">Total Tokens: <strong><%= tokenCount %></strong></p>

  <div class="flex gap-4 mb-6">
    <a href="data:application/json;charset=utf-8,<%= encodeURIComponent(jsonData) %>" download="tokens-<%= date %>.json"
       class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded shadow text-sm">
       ⬇️ Download JSON
    </a>
    <a href="data:text/csv;charset=utf-8,<%= encodeURIComponent(csvData) %>" download="tokens-<%= date %>.csv"
       class="bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded shadow text-sm">
       ⬇️ Download CSV
    </a>
  </div>

  <div class="bg-gray-100 rounded p-4 overflow-x-auto max-h-[600px] text-sm font-mono border border-gray-300">
    <pre><%= jsonData %></pre>
  </div>
</section>

<%- include('partials/footer') %>